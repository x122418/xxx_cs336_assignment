experiment:
  seed: 42

models:
  debug:
    d_model: 128
    d_ff: 512
    num_layers: 2
    num_heads: 4

shared_model:
  vocab_size: 10000
  max_seq_len: 128
  theta: 10000

benchmark:
  batch_size: 1
  sequence_length: 128
  warmup_steps: 2
  measurement_steps: 5

  precision: fp32
  max_grad_norm: null

runtime:
  device: cuda:7